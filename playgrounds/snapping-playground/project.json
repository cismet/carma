{
  "name": "snapping-playground",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "playgrounds/snapping-playground/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/playgrounds/snapping-playground"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "snapping-playground:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "snapping-playground:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "snapping-playground:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "snapping-playground:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "snapping-playground:build:development"
        },
        "production": {
          "buildTarget": "snapping-playground:build:production"
        }
      },
      "dependsOn": ["build"]
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../coverage/playgrounds/snapping-playground"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
