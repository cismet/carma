import panelCss from "./TileLoadingDebugPanels.css?inline";

/** The debugger panel styles as text, for hosts that render their own panels. */
export const TILE_LOADING_DEBUG_PANEL_CSS: string = panelCss;
