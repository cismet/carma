/**
 * Default camera semantics for MapLibre-compatible projections.
 *
 * Kept in the MapLibre engine package so non-engine consumers can reference
 * one framework-owned source of truth for pitch/fov defaults.
 */
export const DEFAULT_MAPLIBRE_PITCH_MIN_DEG = 0;
export const DEFAULT_MAPLIBRE_PITCH_MAX_DEG = 85;
export const DEFAULT_MAPLIBRE_FOV_DEG = 60;
