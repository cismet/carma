/**
 * @fileoverview ElevationControl extension
 *
 * This extension provides a UI control for adjusting camera elevation in the Cesium viewer.
 * Currently disabled by default (show={false}) in CustomViewer.
 *
 * @deprecated Consider removing or refactoring if not actively used.
 */

export { default as ElevationControl } from "./ElevationControl";
