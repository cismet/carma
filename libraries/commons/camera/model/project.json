{
  "name": "camera-model",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libraries/commons/camera/model/src",
  "projectType": "library",
  "tags": ["type:util", "scope:commons"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libraries/commons/camera/model"
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{workspaceRoot}/dist/.vite/camera-model"],
      "options": {
        "config": "libraries/commons/camera/model/vite.config.ts",
        "passWithNoTests": false
      }
    }
  }
}
