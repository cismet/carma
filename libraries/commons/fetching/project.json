{
  "name": "fetching",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libraries/commons/fetching/src",
  "projectType": "library",
  "tags": ["type:util", "scope:commons", "legacy"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libraries/commons/fetching"
      }
    }
  }
}
