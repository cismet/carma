# @carma-commons/interaction-drag

Shared pointer drag helpers for reusable interactive overlays and debug tools.

## Included Helpers

- `DraggableDebugAnchor` crosshair anchor component with pointer-capture drag handling
