export type Metadata = {
  name?: string; // human readable name
  identifier?: string; // some unique identifier not human readable
  description?: string;
  lastUpdated?: string; // last update of the content
  captureDate?: string; // content year
  credits?: string[];
};
