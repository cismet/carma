# sandboxed-eval

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build sandboxed-eval` to build the library.

## Running unit tests

Run `nx test sandboxed-eval` to execute the unit tests via [Vitest](https://vitest.dev/).
